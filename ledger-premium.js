(()=>{const apply=()=>{const main=document.querySelector('.main');if(!main)return;const heading=[...main.querySelectorAll('h1,h2')].map(x=>(x.textContent||'').trim().toLowerCase()).join(' | ');const isLedger=heading.includes('deposits')||heading.includes('bazar schedule');main.classList.toggle('premium-ledger-page',isLedger)};new MutationObserver(apply).observe(document.getElementById('app')||document.body,{childList:true,subtree:true});apply()})();
