<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.Launcher">

        <!-- Digital Asset Links statement, verified against
             https://mess-manager.app/.well-known/assetlinks.json -->
        <meta-data
            android:name="asset_statements"
            android:resource="@string/asset_statements" />

        <meta-data
            android:name="web_manifest_url"
            android:value="@string/web_manifest_url" />

        <activity
            android:name="com.google.androidbrowserhelper.trusted.LauncherActivity"
            android:label="@string/app_name"
            android:theme="@style/Theme.SplashScreen"
            android:exported="true"
            android:configChanges="orientation|screenSize|screenLayout|keyboardHidden">

            <meta-data
                android:name="android.support.customtabs.trusted.DEFAULT_URL"
                android:value="@string/launch_url" />

            <meta-data
                android:name="android.support.customtabs.trusted.STATUS_BAR_COLOR"
                android:resource="@color/colorPrimary" />

            <meta-data
                android:name="android.support.customtabs.trusted.NAVIGATION_BAR_COLOR"
                android:resource="@color/colorPrimary" />

            <!-- A plain PNG under res/drawable, not @mipmap/ic_launcher: that
                 resolves to the adaptive-icon XML on API 26+, which is not a
                 valid source for the raw-bitmap inflation this splash overlay
                 needs (see the note on Theme.SplashScreen in styles.xml for
                 the exact crash this caused). -->
            <meta-data
                android:name="android.support.customtabs.trusted.SPLASH_IMAGE_DRAWABLE"
                android:resource="@drawable/splash_icon" />

            <meta-data
                android:name="android.support.customtabs.trusted.SPLASH_SCREEN_BACKGROUND_COLOR"
                android:resource="@color/backgroundColor" />

            <meta-data
                android:name="android.support.customtabs.trusted.SPLASH_SCREEN_FADE_OUT_DURATION"
                android:value="300" />

            <meta-data
                android:name="android.support.customtabs.trusted.FILE_PROVIDER_AUTHORITY"
                android:value="${applicationId}.fileprovider" />

            <!-- Without this, LauncherActivity has no defined behavior when it
                 can't bind a Custom Tabs provider (Chrome not set as default,
                 disabled, or — as on many Xiaomi/MIUI builds — the binding is
                 blocked outright): it crashes instead of degrading gracefully.
                 "webview" makes it fall back to an in-app WebView, which only
                 needs the system WebView component that's present on every
                 real Android device, Custom Tabs support or not. -->
            <meta-data
                android:name="android.support.customtabs.trusted.FALLBACK_STRATEGY"
                android:value="webview" />

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- Handle links to the verified domain directly inside the app -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <data
                    android:scheme="https"
                    android:host="@string/host_name" />
            </intent-filter>
        </activity>

        <!-- Required, even though nothing here uses "manage space" directly.
             LauncherActivity.launchTwa() unconditionally calls
             ManageDataLauncherActivity.addSiteSettingsShortcut(), which on
             API >= 25 always calls PackageManager.setComponentEnabledSetting()
             on this component (both the "supported" and "not supported"
             branches do, verified in the 2.7.3 bytecode — there is no guard
             and no config flag to skip it). That call throws
             IllegalArgumentException for a component the app doesn't declare,
             which crashed LauncherActivity.onCreate in v1.0.3. The library's
             own AAR manifest deliberately doesn't declare it, so the app must.
             Declared disabled: the library flips it on at runtime only when
             the browser supports site settings. -->
        <activity
            android:name="com.google.androidbrowserhelper.trusted.ManageDataLauncherActivity"
            android:enabled="false"
            android:exported="false" />

        <!-- Lets the TWA delegate web push notifications to the app -->
        <service
            android:name="com.google.androidbrowserhelper.trusted.DelegationService"
            android:enabled="true"
            android:exported="true" />

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/filepaths" />
        </provider>

    </application>
</manifest>
